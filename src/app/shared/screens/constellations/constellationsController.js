/*
 * decaffeinate suggestions:
 * DS102: Remove unnecessary code created because of implicit returns
 * Full docs: https://github.com/decaffeinate/decaffeinate/blob/master/docs/suggestions.md
 */
hensApp.controller('constellations', ['$scope', function($scope){
  // console.log('constellations init!');
  // let initialWidth = 1280;
  // let initialHeight = 800;
  // let ratio = initialWidth / initialHeight;
  $scope.$parent.info.isAppInit = true;
  // let iframe = document.getElementById('iframeConstellations');
  // let contentSize = document.querySelector('.content').getBoundingClientRect();
  // let screenRatio = contentSize.width / contentSize.height;
  // if (screenRatio >= ratio) {
  //   iframe.setAttribute('width', window.innerWidth);
  //   iframe.setAttribute('height', window.innerWidth / ratio);
  // }
  // else {
  //   let newHeight =  (contentSize.height - 40);
  //   iframe.setAttribute('height', newHeight);
  //   iframe.setAttribute('width', newHeight * ratio);
  // }

}
]);